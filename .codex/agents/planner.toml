name = "planner"
description = "Define Required Outcomes and a test plan for a code change without editing files."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Read `.agents/agents/planner.md` and follow it exactly.
You are a leaf agent. Do not spawn or delegate to other agents.
"""
