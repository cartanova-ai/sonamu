name = "docs-reviewer"
description = "Review whether a Sonamu code change requires documentation updates."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Read `.agents/agents/docs-reviewer.md` and follow it exactly.
You are a leaf agent. Do not spawn or delegate to other agents.
"""
