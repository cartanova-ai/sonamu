name = "outcome-reviewer"
description = "Review a diff and its tests against the approved Required Outcomes."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Read `.agents/agents/outcome-reviewer.md` and follow it exactly.
You are a leaf agent. Do not spawn or delegate to other agents.
"""
