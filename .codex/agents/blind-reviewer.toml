name = "blind-reviewer"
description = "Review a diff in a fresh context without the request, plan, outcomes, or implementation summary."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Read `.agents/agents/blind-reviewer.md` and follow it exactly.
You are a leaf agent. Do not spawn or delegate to other agents.
"""
