{
  "private": true,
  "scripts": {
    "build:tools:tasks": "cowsay -f box '@sonamu-kit/tasks 빌드합니다.' && pnpm run --filter @sonamu-kit/tasks build || (cowsay -f explosion '@sonamu-kit/tasks 빌드 실패!' && exit 1)",
    "build:tools:ts-loader": "cowsay -f box '@sonamu-kit/ts-loader 빌드합니다.' && pnpm run --filter @sonamu-kit/ts-loader build || (cowsay -f explosion '@sonamu-kit/ts-loader 빌드 실패!' && exit 1)",
    "build:tools:hmr-hook": "cowsay -f box '@sonamu-kit/hmr-hook 빌드합니다.' && pnpm run --filter @sonamu-kit/hmr-hook build || (cowsay -f explosion '@sonamu-kit/hmr-hook 빌드 실패!' && exit 1)",
    "build:tools:hmr-runner": "cowsay -f box '@sonamu-kit/hmr-runner 빌드합니다.' && pnpm run --filter @sonamu-kit/hmr-runner build || (cowsay -f explosion '@sonamu-kit/hmr-runner 빌드 실패!' && exit 1)",
    "build:tools": "run-s 'build:tools:*'",
    "build:lib:react-sui": "cowsay '@sonamu-kit/react-sui 빌드합니다.' && pnpm run --filter @sonamu-kit/react-sui build || (cowsay -f explosion '@sonamu-kit/react-sui 빌드 실패!' && exit 1)",
    "build:lib:sonamu": "cowsay 'sonamu 빌드합니다.' && pnpm run --filter sonamu build || (cowsay -f explosion 'sonamu 빌드 실패!' && exit 1)",
    "build:lib": "run-s 'build:lib:*'",
    "build:miomock:api": "cowsay 'miomock-api 빌드합니다.' && pnpm run --filter miomock-api build || (cowsay -f explosion 'miomock-api 빌드 실패!' && exit 1)",
    "build:miomock:web": "cowsay 'miomock-web 빌드합니다.' && pnpm run --filter miomock-web build || (cowsay -f explosion 'miomock-web 빌드 실패!' && exit 1)",
    "build:miomock": "run-s 'build:miomock:*'",
    "build": "run-s 'build:tools' 'build:lib' 'build:miomock'",
    "publish-those-not-published-yet": "tsx scripts/publish.ts",
    "format": "biome format --write",
    "check": "biome check",
    "verify:clean": "tsx scripts/verify.ts clean",
    "verify:fast": "tsx scripts/verify.ts fast"
  },
  "packageManager": "pnpm@10.24.0",
  "devDependencies": {
    "@biomejs/biome": "^2.3.7",
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^4.0.12",
    "corepack": "^0.34.1",
    "cowsay": "^1.6.0",
    "npm-run-all": "^4.1.5",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
