{
  "private": true,
  "workspaces": [
    "examples/**/*",
    "modules/**/*",
    "!modules/dump-viewer"
  ],
  "scripts": {
    "build:tools:loader": "cowsay -f box '@sonamu-kit/loader 빌드합니다.' && yarn workspace @sonamu-kit/loader build",
    "build:tools:hot-hook": "cowsay -f box '@sonamu-kit/hot-hook 빌드합니다.' && yarn workspace @sonamu-kit/hot-hook build",
    "build:tools:hot-runner": "cowsay -f box '@sonamu-kit/hot-runner 빌드합니다.' && yarn workspace @sonamu-kit/hot-runner build",
    "build:tools": "run-s 'build:tools:*'",
    "build:lib:react-sui": "cowsay '@sonamu-kit/react-sui 빌드합니다.' && yarn workspace @sonamu-kit/react-sui build",
    "build:lib:sonamu": "cowsay 'sonamu 빌드합니다.' && yarn workspace sonamu build",
    "build:lib:ui": "cowsay '@sonamu-kit/ui 빌드합니다.' && yarn workspace @sonamu-kit/ui build",
    "build:lib": "run-s 'build:lib:*'",
    "build:miomock:api": "cowsay 'miomock-api 빌드합니다.' && yarn workspace miomock-api build",
    "build:miomock:web": "cowsay 'miomock-web 빌드합니다.' && echo '아! miomock-web은 지금 깨져있어서 빌드하지 않고 넘어갑니다! yarn dev를 사용하세요!'",
    "build:miomock": "run-s 'build:miomock:*'",
    "build": "run-s 'build:tools' 'build:lib' 'build:miomock'",
    "publish": "tsx scripts/publich.ts"
  },
  "packageManager": "yarn@4.10.3",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "corepack": "^0.34.1",
    "cowsay": "^1.6.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.6.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
